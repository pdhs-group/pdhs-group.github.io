1. Make sure that NodeJS is installed
2. Make sure the necessary postcss packages are installed with 

npm install --save-dev postcss postcss-cli postcss-nested

3. To build (aka translate css/stlyes.css into css_build/styles.css) use

npm run build:css 

(See package.json for the definition or the full postcss command)